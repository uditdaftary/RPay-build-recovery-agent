# Secrets. This file exists before .env by design, so a key can never be swept in.
.env
.env.local
*.key

# Python
.ruff_cache/
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Runtime artifacts
*.db
*.sqlite3
runs/
audit/*.jsonl
.worktrees/
context.md
.DS_Store
CLAUDE.md

# Debtor contact directory. Deliberately outside data/ledger.json, whose seed-42
# fingerprint is the reproducibility claim, and never committed: real addresses.
data/contacts.json

.vercel
.env*
